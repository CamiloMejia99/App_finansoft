@model IEnumerable<FNTC.Finansoft.Accounting.DTO.MCreditos.CostosPrestamos>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Costos_Adicionales.CA_Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pagare)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Costos_Adicionales.CA_Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pagare)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id_CostoPretamo }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id_CostoPretamo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id_CostoPretamo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
